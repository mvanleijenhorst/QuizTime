<UserControl x:Class="QuizTime.UI.Views.VraagOverzichtUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizTime.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0">Vraag overzicht control</TextBlock>
        <StackPanel  Grid.Row="1" Orientation="Horizontal">
            <Label>Naam van de quiz</Label>
            <TextBox x:Name="QuizNaam" Width="200"></TextBox>
        </StackPanel>

        <DataGrid  Grid.Row="2" Name="Datagrid" />
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <Button Name="NieuweVraag" Content="Nieuwe vraag" Click="NieuweVraag_Click"/>
            <Button Name="QuizOpslaan" Content="Quiz opslaan" Click="QuizOpslaan_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
