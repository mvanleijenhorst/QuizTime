<UserControl x:Class="QuizTime.Wpf.Views.StartUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizTime.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Grid.Row="1" Content="Beheer Quiz" Name="ManageQuiz" Click="ManageQuiz_Click"/>
        <Button Grid.Column="1" Grid.Row="1" Content="Start Quiz" Name="StartQuiz" Click="StartQuiz_Click"/>
        <Button Grid.Column="2" Grid.Row="1" Content="Check Quiz" Name="CheckQuiz" Click="CheckQuiz_Click"/>
    </Grid>
</UserControl>
